rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules allow anyone to read/write data
    // Only use for development/testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}